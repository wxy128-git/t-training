# Firebase → 腾讯服务器迁移进度

当前处于“生产第一阶段已切换、真实邮件链接待用户点击验收”阶段。生产登录会话和站内数据已使用腾讯 MariaDB；Firebase 暂时只承担旧账号首次登录校验和新注册回滚桥接。腾讯本地邮件后端已启用，完全本地注册仍未启用。

## 数据保护与两次快照

- 初始回滚快照：`/home/ubuntu/t-training/backups/20260923-pre-migration-firebase`，包含 Firebase Authentication 358 个账号、Firestore 11 个顶层集合和 10,729 条文档。
- 切换前复核快照：`/home/ubuntu/t-training/backups/20260923-pre-cutover-check`，包含 367 个账号和 10,982 条文档。迁移期间持续出现真实新增账号和文档，均通过后续快照补入。
- 最终只读快照：`/home/ubuntu/t-training/backups/20260924-pre-cutover-final2`，包含 373 个账号、11 个集合和 11,067 条文档；最新腾讯库严格镜像后为 `auth_users` 373、`user_profiles` 365、`firestore_documents` 11,067。
- 两份快照目录权限为 `700`，文件权限为 `600`。最新快照的 12 个数据文件均通过 SHA-256 校验。
- 本地会话签名密钥已随机生成并保存在权限为 `600` 的迁移环境文件中；生成前备份为 `/home/ubuntu/.config/t-training-migration.env.pre-auth-secret-20260923`。生产本地认证和数据开关已启用。
- 两次正式快照导出和镜像导入对 Firebase 只读，没有删除、改写或停用任何既有账号和文档；候选注册桥接回归仅创建并随后删除了一个合成测试账号。

## 已完成的候选实现

- 腾讯 MariaDB 只监听 `127.0.0.1:3306`，账号、资料、原始 Firestore 文档、本地会话和一次性邮箱令牌均保存在独立迁移库。
- 登录使用本地加密会话。旧账号第一次登录时只让 Firebase 校验一次原密码，随后在腾讯库保存新的 `scrypt` 哈希；不保存明文密码。这样不需要额外读取 Firebase 的专用哈希参数，也不会强迫已有用户统一改密码。
- 新注册可完全在腾讯创建 UID、资料和密码，不再依赖 Firebase 注册接口。该能力由环境开关控制，尚未在生产启用。
- 邮箱验证和密码重置已支持腾讯本地一次性链接：令牌只以 SHA-256 摘要入库，30 分钟失效、使用一次后立即作废，重置密码后撤销旧会话。邮件链接直接回到 `ai.teachailab.com`，不需要 VPN。
- 腾讯云已验证发信域名 `notify.teachailab.com` 并创建 `no-reply@notify.teachailab.com`。由于个人实名认证账号不支持 SMTP，生产改用腾讯云 SES API。邮箱验证模板 `61815` 和密码重置模板 `61816` 均已审核通过。专用编程子用户 `t-training-ses` 只关联 `ses:SendEmail` 权限，凭证和两个模板 ID 保存在服务器权限为 `600` 的私密环境文件中；本机下载副本已删除。本地邮件开关已启用。
- Firestore 的备课本、公开内容、工具、后台文案与文章、资源、留言、社区提示词、订阅、统计和用户列表均已有 MariaDB 适配层。前端在本地模式走同源 API，旧 Cloudflare 环境明确回退现有 Firebase 路径。
- 智能体接口可校验本地登录令牌；无效令牌在调用模型前返回 401，不产生模型费用。
- 管理员删除用户在并行迁移阶段仍返回保护性提示，避免只删除一侧导致账号重新出现；正式脱离 Firebase 后再开启本地彻底删除。

## 验证结果

- 项目全量回归通过：11 个公开页、13 个应用页、13 个课程场景、67 项函数断言、79 项邮箱断言、16 项腾讯云 SES 邮件构造断言、19 项生成可靠性断言、15 项腾讯适配断言。
- 候选认证与数据回归通过：备课本 CRUD、文章与工具读取 8 项；管理员读取/保存、权限拒绝、订阅和留言 8 项；登出撤销 3 项。
- 本地注册和邮箱候选回归通过 14 项：注册、生成验证邮件、验证链接单次使用、邮箱状态刷新、密码重置、旧密码失效和新密码登录。
- 两个腾讯云邮件模板审核通过后，以锁定依赖重新部署隔离候选：模拟邮件模式再次通过上述 14 项认证回归，并通过 17 项只读 HTTP 回归；文章、工具和页面文案均确认来自腾讯本地数据层，无效智能体令牌在模型调用前返回 401。复测没有调用模型、没有发送真实邮件。
- 第一阶段候选另通过 16 项 Firebase 注册桥接、本地会话、未验证账号闸门、备课本 CRUD、退出撤销和模型调用前鉴权断言；测试账号已从 Firebase 和腾讯库清理。
- 正式发布目录为 `/home/ubuntu/t-training/releases/20260924-local-phase1-74a8666`，回滚备份为 `/home/ubuntu/t-training/backups/20260924-pre-local-phase1-74a8666`。公网生产检查通过 103 项，候选进程和临时暂存目录已清理。
- 用户授权后向 `xingyuwang124@163.com` 正好发出一封验证邮件和一封密码重置邮件，腾讯云 SES 均返回发送编号；两枚 30 分钟一次性令牌目前各一枚有效，等待用户点击验收。
- 候选智能体使用无效令牌返回 401，未发起模型请求。
- 候选产生的测试账号、留言、订阅、会话、邮箱令牌和远端临时脚本均已清理；真实投递验收所需的两枚邮件令牌按设计保留至使用或 30 分钟后失效。

## 完全本地注册前仍需完成

1. 在 `xingyuwang124@163.com` 收件箱完成验证链接和密码重置链接点击，确认无需 VPN、页面正常、令牌只能使用一次。
2. 确认邮件是否进入垃圾箱、发件人和正文显示是否正常。
3. 验收通过后启用完全本地注册，停止创建新的 Firebase 账号；已有账号首次登录迁移桥接继续保留一段观察期。
4. 观察生产登录、注册、备课本和管理后台后，再评估何时移除 Firebase 首次登录桥接与管理员删除保护。

迁移脚本见 `migration/export-firebase.mjs`、`migration/import-firebase.mjs` 和 `migration/schema.sql`。导出脚本只读 Firebase；导入默认只做 upsert，只有显式设置 `T_TRAINING_IMPORT_RECONCILE=1` 才会把隔离库严格对齐到快照，避免误删本地新增数据。
